{
  "builds": [
    {
      "src": "ui/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "ui/dist" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/ui/$1" }
  ]
}
